# つかいきり 進捗管理

最終更新: 2026-06-14（コードレビュー＝オンデバイス AI 移行・指摘10件すべて対応。#1〜#6 バグ修正 + #8〜#10 整理 + #7 部分対応）

本書はタスクの進捗と要検討事項を管理する。機能仕様は [仕様書.md](仕様書.md)、実装方式は [設計書.md](設計書.md) を参照。**作業の完了・着手・方針決定のたびにこのファイルを更新すること。**

凡例: ✅ 完了 / 🚧 進行中（一部完了） / ⏸ ブロック中 / 🔜 未着手

---

## 1. フェーズ別タスク一覧

| # | フェーズ | 状態 | 備考 |
|---|---|---|---|
| 1 | Flutter プロジェクト作成（macOS + iOS + Android） | ✅ | CocoaPods 除去・SPM 化済み |
| 2 | 在庫管理 最小版 + i18n 土台 | ✅ | 在庫一覧・追加/編集・詳細・設定（言語）の UI まで完成 |
| 3 | 買い物リスト連携（macOS/iOS） | 🚧 | サービス層 ✅ / macOS UI ✅（M5）。実機リマインダーでの動作確認が未了 |
| 4 | AI プロバイダ抽象化 + Gemini | 🚧 | サービス層 ✅ / macOS 献立提案 UI ✅（M4）。実 API での動作確認が未了 |
| 5 | 追加プロバイダ（Gemini → Grok → OpenAI → Claude の優先順位） | 🚧 | 全4社のサービス層 ✅・モデル一覧 API ✅ / プロバイダ・モデル選択 UI ✅（M3）。実 API での動作確認が未了 |
| 6 | 所有家電に応じた調理手順（Hotcook / Healsio） | ✅ | プロンプト実装（種別+容量・汎用モード名・向く料理だけ家電）・フェイク検証済み。実 API での出し分け品質確認はユーザーToDo 1 番 |
| 7 | カメラ登録（半自動） | 🚧 | 認識 API ✅・macOS UI ✅（M6: 画像選択/D&D→縮小→解析→確認画面→在庫保存、テスト9件）。実 API での認識品質確認が未了（ユーザーToDo 1 番） |
| 8 | データ同期（SyncService: iCloud / Google Drive） | 🚧 | macOS iCloud 実装 ✅・**実機検証 ✅**（2026-06-11: バックアップ/自動バックアップ/復元/ループなしを実機 iCloud で確認）。残は iOS 確認と Android（Google Drive） |
| 9 | ヘルプ/このアプリについて + ドネーション | 🔜 | FoodKeeper 出典表記・Buy Me a Coffee リンク |

### macOS 以降の展開（フェーズ横断）

| タスク | 状態 | 備考 |
|---|---|---|
| iOS 対応（RemindersPlugin の iOS 登録・実機テスト） | 🔜 | EventKit 実装は macOS/iOS 共通。実機テストに Apple Developer アカウントが必要 |
| Android 対応（Google Tasks / Google Drive / Keystore） | 🔜 | ShoppingListService の Google Tasks 実装から |

---

## 2. 直近の完了タスク（基盤レイヤー）

- ✅ 2026-06-13: ヘルプ画面に AI 文言を追加【F】（TDD）
  - `help_content.dart`（desktop/mobile 共有）の「使い方」直後に「AI について」ブロックを追加：本文（対応端末ではオンデバイスで動作・無料・キー不要・オフライン / より賢い提案は設定で自前キー）+ 緑のプライバシー Callout（写真・在庫は端末内処理で外部送信なし）。
  - l10n（en/ja/es）に `helpAiEyebrow`/`Title`/`Body1`/`Body2`/`CalloutTitle`/`CalloutBody` を追加。
  - テスト: help_desktop_view_test の主要見出しテストに「AI について」を追加 + 専用テスト（オンデバイス説明・プライバシー表示）。
  - 検証: **flutter analyze 0 / 全 245 テスト / macOS debug ビルド 成功**。
  - 備考: 以前は「オンデバイス実装と同時に入れる」として保留していた文言。B1/B2 実装済みのため正確に追記できた。これでオンデバイス AI 方式転換に伴う一連のタスク（設計メモ/設計書、B1 Apple、B2 Android、A 既定解決、プロバイダ選択 UI、C オンボーディング、E ゲーティング、F ヘルプ）が完了。

- ✅ 2026-06-13: AI 非対応端末での AI 機能無効化＋案内【E】（TDD）
  - 要件: オンデバイス非対応かつ自前キー未登録の端末（例: Gemini Nano 非対応 Android）で、AI 専用画面（献立提案・カメラ登録）を無効化し案内。在庫・買い物リストは通常利用可。
  - `aiAvailableProvider`（FutureProvider<bool> = `recipeProviderProvider` が非 null）を追加。共有 `AiUnavailableNotice`（`recipe/presentation/ai_unavailable_notice.dart`）+ l10n `aiUnavailableTitle/Body`（en/ja/es）。
  - 4 view で入口を出し分け: meals desktop（提案ボタン無効化 + before 状態を案内に）/ meals mobile（条件・CTA を案内に差し替え・戻る維持）/ camera desktop（capture を案内に差し替え）/ camera mobile（戻るヘッダー維持で案内）。解決中は `aiAvailable=true` 既定で誤フラッシュ防止。
  - テスト: meals_desktop / camera_desktop に「provider=null 上書き → 案内表示・入口無効」を追加。既存テストは fake provider 上書きで aiAvailable=true のため不変。
  - 検証: **flutter analyze 0 / 全 244 テスト / macOS debug + Android debug APK ビルド 成功**。
  - 設計書 §5.1.1 に AI 利用可否ゲーティングを反映。残: F(ヘルプ AI 文言)。

- ✅ 2026-06-13: オンボーディングから API キー入力を削除【C】（TDD）
  - 方針: オンデバイス AI 既定のため、初回フローでは API キー入力・プロバイダ選択をしない。オンデバイス可否のみ表示し、使えない場合だけ「設定からご自分のキーを登録すればクラウド AI」を案内。家電・買い物リスト等の既存ステップは不変。
  - desktop `_AiStep` / mobile `_MobileAiStep` を共有 `OnDeviceStatusCard`（`onDeviceAiAvailabilityProvider` を watch。可=「{name} が端末内で動作…」/ 不可=案内）に置換。完了サマリーの `_resolveAiName` に `'ondevice'`→`onDeviceDisplayName()` 追加。
  - l10n（en/ja/es）: `onboardingAiTitle`/`Sub` を更新、`onboardingAiOnDeviceReady({name})`/`onboardingAiOnDeviceMissing` を追加。
  - 旧 `OnboardingProviderGrid`/`OnboardingApiKeyCard` は未使用化（public のため analyze 非対象。境界に共有ヘルパー混在のため今回は残置 → 将来削除）。
  - テスト: onboarding ① ② ④ のタイトル文言更新、③ を「オンデバイス可なら準備OK表示・プロバイダ選択は非表示」に書換（pumpView に availability override 追加）。
  - 検証: **flutter analyze 0 / 全 242 テスト / macOS debug ビルド 成功**。
  - 設計書 §5.1.1 にオンボーディング AI ステップの設計を反映。残: E(Android 非対応端末の AI 無効化 UI)・F(ヘルプ AI 文言)。
  - 追加（同日）: 残置した未使用クラス `OnboardingProviderGrid`/`_OBProviderCard`/`OnboardingApiKeyCard`/`_OnboardingApiKeyCardState` を**慎重な依存検証の上で削除（381行）**。アンカー文字列ベース削除 + アサーション（`OnboardingApplianceCard` 非巻き込み確認）。連鎖で `_OBSmallButton.primary`（未使用化）と `url_launcher` import を除去。flutter analyze 0 / 全 242 テスト / macOS ビルド 成功で誤削除なしを確認。

- ✅ 2026-06-13: オンデバイス AI【B2】Android（Gemini Nano / AICore）実装
  - Dart を platform-neutral 化: `AppleFoundationModelsProvider`→`OnDeviceRecipeProvider`（file は git mv）。channel・プロンプト・パースは iOS/macOS と完全共通。`onDeviceDisplayName()`（`recipe_provider_factory.dart`：Android→'Gemini Nano' / 他→'Apple Intelligence'）を追加し provider と `providerDisplayInfo('ondevice')` で共用。`recipeProviderProvider`・関連テスト（型名・modelId='on-device'）を更新。
  - Android native: `android/app/src/main/kotlin/.../OnDeviceAiPlugin.kt`（同 channel `…/ondevice_ai`）が **AICore `GenerativeModel.generateContent`**（Gemini Nano）を呼ぶ。`availability` は AICore システムアプリ（`com.google.android.aicore`）の有無 + `SDK_INT>=34` を軽量判定（DL を伴わない）。`MainActivity.configureFlutterEngine` で登録。`build.gradle.kts` に `com.google.ai.edge.aicore:aicore:0.0.1-exp01`。
  - **minSdk 24→31**（AICore 要求。ユーザー承諾済み）。Android 12 未満は対象外に。
  - 設計書 §5.1.1 を Android 実装内容に更新。
  - 検証: **flutter analyze 0 / 全 242 テスト / macOS debug ビルド / Android debug APK ビルド すべて成功**（AICore API が実コンパイル通過）。
  - 制約: 現状テキスト専用 → `supportsVision=false`（Android オンデバイスではカメラ登録不可）。実機（Gemini Nano 対応端末）での生成品質・ダウンロード挙動の確認はユーザー ToDo。
  - 残: C(オンボーディングの API キー入力削除)・E(Android 非対応端末の AI 無効化案内 UI)・F(ヘルプ AI 文言)。

- ✅ 2026-06-13: プロバイダ選択にオンデバイス表示 + 端末依存の既定（TDD）
  - 仕様変更: AI プロバイダ選択画面の**先頭にオンデバイス（Apple Intelligence）**を表示し明示選択可能に。**非対応端末はグレーアウトして選択不可**。**既定値は初回起動時に端末で判定**（対応→`'ondevice'` / 非対応→`'gemini'`）。
  - ①仕様書 §6.2 + 設計書 §5.1.1 に上記を反映。
  - ②Red→③Green: `SettingsRepository.initializeDefaultProviderIfUnset(id)`（設定行が無いときだけ既定保存・初回のみ）をテスト先行で追加。`onDeviceAiAvailabilityProvider`（FutureProvider）追加。`main()` を `UncontrolledProviderScope` 化し、起動時 availability から既定を初回設定。
  - UI: desktop `_ProviderGrid`/`_ProviderCard` を「先頭オンデバイス + `enabled` でグレーアウト + 選択中はキー/モデル欄を隠し注記表示」に書き換え。mobile `ai_settings_screen` に `_onDeviceRow`（先頭・非対応は Opacity+onTap無効）追加、同じ出し分け。l10n（en/ja/es）に `settingsAiOnDeviceDesc`/`Unavailable`/`NoKeyNote` 追加。
  - テスト: settings_repository（initialize 2件）+ settings_desktop_view（対応=選択でキー欄消える / 非対応=グレーで選択不可 の2件）。
  - 検証: **flutter analyze 0 / 全 242 テストパス / macOS debug ビルド成功**。
  - 備考: DB の static 既定 `selectedProvider='gemini'` は維持（初回判定の最終フォールバックを兼ねる）。実際の既定は initialize が端末で上書き。

- ✅ 2026-06-13: 既定プロバイダ解決ロジック【A】（TDD: 仕様書→Red→Green→Refactor）
  - ①仕様書: `doc/仕様書.md` §6.2 に「AI プロバイダの選択（2段構え・オンデバイス既定・解決順）」を追記。`doc/設計書.md` §5.1.1 の解決を「✅ 実装済み」に更新（'ondevice' sentinel／既定値は当面 'gemini' 維持＝設定UI再構成は別タスクで UI と同時、の理由を明記）。
  - ②Red→③Green: `recipe_provider_resolution_test.dart`（5ケース）を先に書いて失敗確認→実装。`recipeProviderProvider` を解決順【①クラウド選択＆キー有り→そのクラウド ②キー無し/`'ondevice'`選択でオンデバイス可→`AppleFoundationModelsProvider`（supportsVision 引継ぎ）③不可→null】に書き換え。`onDeviceAiServiceProvider`（DI・差し替え可）追加。`onDeviceProviderId='ondevice'` 定数 + `providerDisplayInfo('ondevice')`='Apple Intelligence'。
  - 規約改善: 旧実装の `userSettingsProvider.future`（非 UI で `.future` 非推奨）を、`ref.watch(userSettingsProvider)`（再解決依存）+ `settingsRepository.get()`（一発クエリ）に変更（CLAUDE.md テスト規約準拠・テスト容易化）。
  - 検証: **flutter analyze 0 / 全 238 テストパス**。Dart のみの変更（ネイティブ不変）。テスト環境ではオンデバイスが MissingPlugin→unavailable のため、既存の「キー無し→null」挙動は維持。
  - 残（次タスク）: B2(Android Gemini Nano)・C(オンボーディングの API キー入力削除)・D(設定 UI 再構成＋既定を 'ondevice' へ)・E(Android 非対応端末の AI 無効化案内)・F(ヘルプ AI 文言・オンデバイス実装と同時)。

- ✅ 2026-06-13: オンデバイス AI（macOS/iOS = Apple Foundation Models）実装【B1】
  - 設計書（`doc/設計書.md`）§5.1 を 2 段構えに更新 + §5.1.1「オンデバイス AI」を追加（方式・channel・Swift ガード・Vision の扱い）。§2 技術スタックにオンデバイス AI 行を追加。
  - Dart: `OnDeviceAiService`（channel `com.futo4.tsukaikiri/ondevice_ai` のラッパ。`availability()`→{available, supportsVision, reason} / `generate(prompt, images?)`→String。`OnDeviceAiException`）。`AppleFoundationModelsProvider implements RecipeProvider`（プロンプト生成・JSON パースは `recipe_prompts.dart` を**クラウドと完全共有**、ネイティブは「プロンプト→テキスト」のみ。supportsVision は availability から注入、displayName='Apple Intelligence'、modelId='apple-foundation-models'、失敗は `RecipeProviderException('ondevice',0,…)`）。
  - Swift: `OnDeviceAiPlugin.swift`（macos/Runner + ios/Runner に同一実装。`#if canImport(FoundationModels)` + `@available(macOS 26 / iOS 26)` でガードし未対応環境は available=false に縮退。`SystemLanguageModel.default.availability` で可否判定、`LanguageModelSession().respond(to:)` で生成）。macOS=`MainFlutterWindow`、iOS=`AppDelegate.didInitializeImplicitFlutterEngine` で登録。両 `project.pbxproj` にファイル追加。
  - テスト: `on_device_ai_provider_test`（8件: メタ情報・suggest/normalize/recognize パース・vision 無効時 UnsupportedError・例外/壊れJSON→RecipeProviderException）+ `on_device_ai_service_test`（5件: availability 解釈・MissingPlugin 縮退・generate・PlatformException→OnDeviceAiException）。
  - 検証: **macOS debug ビルド成功 / iOS simulator ビルド成功 / flutter analyze 0 / 全 233 テストパス**（Swift6 Sendable 警告も解消）。
  - 制約: 現状オンデバイス基盤モデルはテキスト専用 → **supportsVision=false**（オンデバイスではカメラ登録を無効化。画像認識は将来のマルチモーダル/Vision framework）。実機での生成品質確認は Apple Intelligence 有効端末のユーザー確認が必要（platform channel はテスト環境で生成不可）。
  - 次タスク【A】: 既定プロバイダ解決ロジック（自前キー優先→オンデバイス→無効化）・`'ondevice'` sentinel・`recipeProviderProvider` 配線。オンデバイス実装済みのため既定切替が安全に可能。

- ✅ 2026-06-13: AI 方式の設計転換（オンデバイス既定・キー不要無料）を設計メモに反映
  - 背景: 一般ユーザーに API キー取得・入力は離脱要因。「インストールしてすぐ・キー不要・無料・オフライン」を既定にする方針へ転換。
  - 設計メモ更新（`doc/献立アプリ_設計メモ.md`）: コンセプトに「オンデバイス既定」を明記 / 「AI プロバイダ」を**2段構え**（段階1=オンデバイス[iOS/macOS=Apple Foundation Models・Android=Gemini Nano]、段階2=任意の自前キー）に再構成 / **VPS プロキシ（サブスク route）を削除** / 既定プロバイダ解決ロジック（自前キー優先→オンデバイス→無効化）を追加 / Android 非対応端末は AI 無効化 + 案内 / 課金・コスト戦略・オンボーディング・データモデル・技術構成・開発の進め方・ヘルプ各セクションを整合。
  - **残コード作業（未実装・今後）**:
    1. オンデバイス実装の追加: `AppleFoundationModelsProvider`（platform channel → Swift Foundation Models、構造化出力 `@Generable`/`@Guide`、マルチモーダル）/ `GeminiNanoProvider`（Android ML Kit GenAI Prompt API、コンテキスト ~4096 トークン配慮、JSON 出力）。いずれも `RecipeProvider` 抽象に乗せる。
    2. 既定プロバイダ解決の再実装: `recipeProviderProvider` を「自前キー優先 → オンデバイス可否 → 無効化」に。`selectedProvider` に「オンデバイス既定」値を表現（現状は claude/openai/gemini/grok のみ）。
    3. オンボーディングから API キー入力ステップを削除（オンデバイス可否判定のみ・使えない場合だけ案内）。自前キーは設定「詳細」へ。実装済み Onboarding（desktop/mobile）の改修が必要。
    4. Android 非対応端末（Gemini Nano 非対応）で AI 機能（献立提案・カメラ登録）を無効化し案内する分岐。
    5. ヘルプ画面に「AI は端末内で動作・無料・オフライン・写真や在庫は端末外に出ない」旨を追記（**オンデバイス実装と同時に入れる**。先に入れると現状の挙動と food わず誤情報になるため保留）。
    6. CLAUDE.md ロードマップ#5（マルチプロバイダ Gemini→Grok→…）の記述を 2 段構えに合わせて見直し。
  - 注記: 本タスクは設計メモ（仕様）更新のみ完了。上記コードは大きめのネイティブ作業を含むため別タスクで段階的に着手する。

- ✅ 2026-06-13: Grok 作業（未コミット分）の検証・是正・完成
  - 背景: レートリミット中に Grok が Help/Onboarding モバイル・「レシピを見る」focus 機能・スペイン語追加・カメラ/同期 UX 基盤の DB スキーマを進めたが未コミット。検証で「analyze 0・全機能完成」記述が不正確と判明（実際 analyze 5件、スペイン語は英語丸コピーで未翻訳、v4列はデッドスキーマ）。
  - 是正(A): analyze 5件をゼロに（未使用 `toast`/`l10n`/import 除去、`meta` を pubspec dependencies に追加、テスト文字列補間）。focus バナーのハードコード日本語 `「{name}」から提案中` を `mealsFocusBanner` l10n キー化（desktop/mobile・en/ja/es）。
  - スペイン語(B): `app_es.arb` 全 453 キーを実翻訳（英語コピーから差し替え）。`app_es.arb.bak` を git clean で除去。`flutter gen-l10n` で再生成（untranslated 警告0）。**AI 出力言語は ja/en フォールバックのまま**（prompts 拡張は別途）。
  - v4 機能完成(C): `cameraPreserveState`/`syncKeepOnFailure` を実機能化。`SettingsRepository._toSettings` が両列を読むよう修正（デッドスキーマ解消）＋ setter 2つ追加。**既定値を現行挙動に整合**（cameraPreserveState 既定を false→**true**=保持に変更、DB/UserSettings/repository を統一）。カメラ mobile の入場リセットを `cameraPreserveState` でゲート（false=毎回リセット、true=途中保持・error のみリセット）。同期トグル ON 直後のバックアップ失敗時、`syncKeepOnFailure=false` なら OFF へ巻き戻し（desktop/mobile 両ハンドラ）。設定データセクションに 2 トグル UI を追加（desktop `_ToggleRow` / mobile Switch 行・en/ja/es 文言）。
  - テスト追加: settings_repository（setter 往復・既定 true）、data_settings（keep=false で失敗時 OFF へ巻き戻し／既定で ON 維持。既存トグルテストは Switch 3個化に伴い `.first` で特定）、camera_mobile（preserve true/false の review 再入場挙動）。
  - 品質: **flutter analyze 0 / 全 220 テスト パス**（サンドボックス無効で実行）。コード生成（build_runner・gen-l10n）反映済み。コミット対象外: `review.md`（レビュー入力アーティファクト）。

- ✅ 2026-06-13: バグ・エッジケース静的スキャン (full static review)
  - 対象: 4コントローラ (meal_suggestion/shopping_confirm/camera_capture/sync)、全フロー (suggestRecipes/camera addImages+supportsVision+confirm/shopping missing+add/sync backup-restore)、4 AI プロバイダ+recipe_prompts、2 native plugin (Reminders/ICloud)、shelf_life/norm/secure/error/offline など指定全領域。
  - ツール多用 (list_dir/grep 多数/read_file 20+ファイル) で全検証。CLAUDE.md 規約 (repo query 厳守、_isRestoring 安全、no keys backup 等) 遵守確認。
  - **高信頼度指摘 3件のみ抽出** (他は設計忠実・防御的実装確認)。詳細は notes.md + 別 writeup。
  - 追加: notes.md 開始前/30分毎/完了時更新 (3回)、進捗本ファイル更新。
  - 品質: flutter analyze 0、既存テスト全パス前提 (実API未検証は既知)。
- ✅ 2026-06-13: レビュー指摘4件の即時修正 (review.md ベース)
  - 1: `camera_desktop_view.dart` のプライベート `_catEmoji` マップ（回帰）を完全除去。`../../inventory/domain/category_style.dart` インポート + `c.category.style.emoji` / `sel.category.style.emoji` / `cat.style.emoji` に置換（inventory_desktop・ingredient_detail・camera_mobile と完全一致。meat🍖/dairy🥛 など統一）。
  - 2: `core/providers.dart` の `recipeProviderProvider` に `if (!supportedProviderIds.contains(providerId)) return null;` ガード追加（未知/legacy providerId で createRecipeProvider の ArgumentError を避け、コントローラの network 誤分類も防止）。`backup_codec.dart` の `_decodeSettings` でも rawProvider を supported チェックして 'gemini' フォールバック（restore 耐性強化。providerDisplayInfo と同等）。
  - 3: `camera_capture_controller.dart` の `confirm()` 保存ループ（expiryFromName → repo.save → _reset）を try { for... } catch { ... } finally { _resetToCapture(); } で包む。analyze() は例外完備だったが confirm は欠如 → 部分保存+review残留リスクを低減（DBエラー稀だがM6主要フロー）。
  - 4: `test/ingredient_form_screen_test.dart` の `unmountApp` を `await tester.pump(const Duration(milliseconds: 1));` + 詳細コメント（「引数なしpumpは...」説明 + addTearDown注意）に統一（inventory_list_screen_test と完全一致。2026-06-12の「unmount 1ms統一」適用漏れ修正）。
  - 影響: すべて低コスト・既存テストで回帰容易に守れる。視覚一貫性・restore後AI使用・カメラ確定堅牢性・テストスイート安定性の4点が改善。
  - 手順: 修正→notes中間更新（30分ルール）→flutter analyze 0 → flutter test バックグラウンド実行 → 進捗/ notes 完了更新 → push通知。CLAUDE.md「作業の完了・着手のたびに更新」遵守。
- ✅ 2026-06-13: 修正内容のコミット & プッシュ
  - コミット: c9235b4 `fix: レビュー指摘4件の修正 + ドキュメント更新`（7ファイル: lib/4 + test/1 + notes.md + 進捗管理.md）。
  - 対象外: review.md（untracked のまま。レビュー入力アーティファクトのため history に混入せず）。
  - プッシュ: `git push` 正常完了（1410811..c9235b4 → origin/main）。リモート反映確認。
  - メッセージ: 4指摘の具体的内容 + CLAUDE.md 遵守明記。notes.md に全工程記録済み。
  - 品質: コミット前に全テストパス・analyze 0 を再確認。ビルド成果物や不要ファイルは一切含まず。
  - 追記: notes.md に「コミット&プッシュ タスク」セクションを追加（開始前記録・判断・結果）。進捗管理も本エントリで更新。最終 curl push 通知で完了報告。
- ✅ 2026-06-12: コードレビュー（7角度）指摘の修正
  - 挙動修正: ①買い物エラー画面の「もどる」が「もう一度試す」と同一動作だった → エラー解除+pop に修正 ②未知の selectedProvider（旧版設定・壊れたバックアップ由来）で設定画面がクラッシュ → `providerDisplayInfo()`（フォールバック付き）に統一 ③カメラ（モバイル）再入場時に過去のエラー画面が残存 → error のみ入場時自動リセット ④同期トグル ON で即時+デバウンスの二重バックアップ → 即時バックアップ後にスケジュールを取消（両プラットフォーム） ⑤在庫詳細の買い物リスト追加失敗文言を権限言及ありの `shoppingErrorNetwork` に変更
  - 重複解消: `providerKeyUrls`/`providerDisplayInfo` を recipe_provider_factory に集約（3ファイルの重複削除）/ 家電の型・容量定数を appliance.dart に集約 / `formatDateTimeMinutes` を core/utils に / `MobileNavBackButton` を core/widgets に（モバイル3画面の重複削除）/ カメラの絵文字マップを `category.style.emoji` に統一（発散していた）/ テストの FakeSecureStorage を test/fakes に統合
  - 簡素化: 買い物モバイルの到達不能な ref.listen 削除（テストはこのデッドパスに依存していたため本番同順の set→構築に修正）/ data_settings の lastSyncedAt 手動 State を watch 導出に / 未使用 l10n キー2件削除
  - 未修正（§4 にユーザー判断として追加）: カメラ途中再開の是非・同期トグル失敗時の巻き戻し
  - リグレッションテスト2件追加（買い物もどる pop・カメラ error リセット）。計 200 件全パス・analyze 0

- ✅ 2026-06-12: モバイル設定画面の実機能化（サポート以外）
  - サブ画面方式（LanguageDetailScreen と同パターン）でデスクトップ版と同じデータ操作を移植。コントローラ・リポジトリは共有
  - `ai_settings_screen.dart`（プロバイダ4社選択・API キー保存/マスク/削除・モデル一覧取得と上書き）/ `integration_settings_screens.dart`（買い物リスト読み込み/選択/新規作成・調理家電トグル+型/容量チップ）/ `data_settings_screen.dart`（自動バックアップトグル[ON即時バックアップ]・最終日時・手動バックアップ/復元+確認ダイアログ。`SyncController` 共有）
  - メイン設定画面は各行に現在値（プロバイダ名・リスト名・所有家電・同期状態）を表示して遷移
  - 残スタブはサポート行のみ（Buy Me a Coffee=フェーズ9のURL待ち・ヘルプ・About）
  - widget テスト 14 件追加（AI 4 / 連携 6 / データ 4）。計 198 件全パス・analyze 0
  - 学び: 画面に drift stream watch を追加したら、その画面の既存テストの unmountApp が `pump()`（経過0）だと pending timer で落ちる → `pump(1ms)` に統一

- ✅ 2026-06-12: モバイル幅 UI 拡充（献立提案・買い物リスト確認・カメラ登録）
  - iOS 対応の前提作業。デスクトップ版のコントローラ（`MealSuggestionController` / `ShoppingConfirmController` / `CameraCaptureController`）を共有し、view のみ新規実装（ロジック複製なし）
  - `meals_mobile_view.dart`（mealsPhone.jsx 準拠・カード展開で材料/手順・「これ作る」トグル・買い物リストへCTA）/ `shopping_mobile_view.dart`（shoppingPhone.jsx 準拠・チェック/数量/全選択・追加先選択+新規作成・完了/エラー）/ `camera_mobile_view.dart`（camera.jsx 準拠・写真追加[当面 file_selector、iOS フェーズで image_picker に差し替え予定の関数分離]・候補カード展開編集・確定で在庫保存）
  - 在庫画面（モバイルホーム）の `onSuggest` / `onCamera`（空状態の「カメラで登録」含む）を comingSoon から実遷移に差し替え
  - widget テスト 16 件追加（meals 4 / shopping 5 / camera 7）。計 181 件全パス・analyze 0
  - 残りのモバイル版: ヘルプ・オンボーディング（設定アシスタント）。在庫詳細の「買い物リストに追加」「レシピを見る」ボタンも comingSoon のまま

- ✅ 2026-06-11: フェーズ8 データ同期 macOS iCloud 実装
  - **決定事項（ユーザー判断）**: 保存先 = iCloud ubiquity コンテナの隠し領域 `backup/tsukaikiri_backup.json`（単一 JSON 上書き・ユーザーから不可視）/ トリガ = 自動（在庫・設定変更後5秒デバウンス、`syncEnabled` トグルで ON/OFF）+ 手動ボタン併設 / 復元 = 手動+確認ダイアログのみ / ファイルへのエクスポート・インポートは作らない（iCloud 一本化）/ API キーはバックアップに含めず iCloud キーチェーン同期（`synchronizable: true`）に任せる
  - `SyncService` 抽象（`isAvailable` / `writeBackup` / `readBackup`）+ `ICloudSyncService`（`icloud_sync_service.dart`）: platform channel `com.futo4.tsukaikiri/icloud_sync` 経由でネイティブ呼び出し。`not_signed_in` → `SyncNotSignedInException`、その他 → `SyncIOException`
  - `ICloudSyncPlugin.swift`（新規）: ubiquity コンテナへの読み書き（`NSFileCoordinator`・バックグラウンドスレッドでコンテナ解決）。**未ダウンロード対応**: 実ファイルが無くてもプレースホルダ（`.〜.icloud`）を検出して `startDownloadingUbiquitousItem` → 最大10秒ポーリング（新しい Mac での初回復元シナリオ）
  - `SyncController`（Riverpod v3 `Notifier<SyncState>`）: `backup()` / `restore()` / `applyRestore()` / `silentBackup()` + `_isRestoring` フラグ（復元中バックアップ抑止）。**状態は文言でなく種別 enum**（`SyncSuccessKind` / `SyncErrorKind`）で持ち、表示文言は view が l10n で解決（i18n 規約・カメラ実装と同パターン）
  - `BackupScheduler`（デバウンス。既定5秒・テストでは短縮値を注入可能）+ `autoBackupWatcherProvider`（`main.dart` で常駐）。**ループ防止**: バックアップ自身の `lastSyncedAt` 更新で設定 stream が発火するため、設定側は lastSyncedAt を除いた指紋が変わったときだけスケジュール（これがないとバックアップが5秒間隔で永久に続く）
  - entitlements 2ファイル: `read-write` → `read-only` に巻き戻し、iCloud 3エントリ追加（`icloud-container-identifiers` / `icloud-services=CloudDocuments` / `ubiquity-container-identifiers` = `iCloud.com.futo4.tsukaikiri`）
  - `_DataSection` を iCloud 仕様に全面書き換し（syncEnabled トグル[ON 直後に即時バックアップ]・今すぐバックアップ・復元ボタン+確認ダイアログ[日時・件数]・最終バックアップ日時）
  - `secure_storage_service.dart`: API キーを `synchronizable: true` で読み書き（iCloud キーチェーン同期）+ 旧アイテムからの自動移行 + 開発署名での失敗時フォールバック
  - テスト 19 件追加: `backup_codec_test`（5）/ `backup_scheduler_test`（3）/ `settings_repository_sync_test`（4）/ `auto_backup_watcher_test`（3: ループしないこと・在庫変更で発火・OFF で発火しない）/ settings_desktop_view に widget 4（バックアップ成功+APIキー非含有・iCloud 不可エラー・復元ダイアログ→在庫置換・キャンセル）。計 165 件全パス
  - `flutter analyze`: 0 issues。コンパイル検証: `xcodebuild CODE_SIGNING_ALLOWED=NO` で BUILD SUCCEEDED
  - **実機検証 ✅（2026-06-11）**: ユーザーが Xcode で Team 署名（LT37BHQT62・Automatically manage signing・iCloud capability）→ 実機 iCloud で確認: ①トグル ON → 即時バックアップが ubiquity コンテナに出現（formatVersion 1・API キー非含有） ②在庫追加 → 5秒デバウンスの自動バックアップ（ShelfLifeTable の期限プレフィルも実データで確認） ③3件→復元→2件と**復元でデータが巻き戻ることを目視確認** ④復元後のバックアップは1回で静止 = **無限ループなしを実機確認**
  - ~~既知の制約: この開発環境（Claude Code のシェル）はキーチェーン UI が使えず `codesign` が errSecInternalComponent で失敗する~~ → **✅ 解決（2026-06-12）専用署名キーチェーン方式**

- ✅ 2026-06-12: macOS 起動時にデスクトップ UI で開くよう初期ウィンドウサイズを修正
  - 症状: 初期ウィンドウが xib 既定の 800×600 で `kDesktopBreakpoint`（1000px）未満のためモバイル UI で起動していた
  - `MainFlutterWindow.swift`: 保存フレームがない初回起動は 1280×850 で中央表示、以後は frame autosave（`MainWindow`）でユーザーのサイズ・位置を復元
  - 落とし穴: macOS の状態復元（`restoreStateWithCoder:`）が起動約3秒後に過去フレームで上書きしていたため `isRestorable = false` を併用（スタックトレースで特定）
  - 実機検証: クリーン起動で 1280×882 のまま安定（= デスクトップ UI）

- ✅ 2026-06-12: 開発環境シェルからの macOS 署名問題を解決（専用署名キーチェーン）
  - 原因の確定: Claude Code のシェルは Background launchd セッション（`launchctl managername` = Background）にあり、login キーチェーンがセッション単位でロック + UI ダイアログ禁止 → 鍵 ACL を緩めても `codesign` が `errSecInternalComponent` で失敗していた
  - 対処（CI 標準手法）: ユーザーのターミナルで `tool/macos_signing/setup_signing_keychain.sh` を1回実行 → login から Apple Development identity を .p12 経由で専用キーチェーン `~/Library/Keychains/tsukaikiri-ci.keychain-db` に複製（`-T /usr/bin/codesign` + partition list 設定で非対話署名可）。パスワードはランダム生成し `~/.config/tsukaikiri/ci-keychain-pass`（リポジトリ外・chmod 600）に保存
  - 追加の発見: ビルド/署名検証は**サンドボックス内では信頼チェーン評価が trustd に届かず失敗する**（`CSSMERR_TP_NOT_TRUSTED` / find-identity が 0 valid）。署名を伴うコマンドはサンドボックス無効で実行する
  - 検証: 開発環境シェルから `flutter build macos --debug` 成功（TeamIdentifier=LT37BHQT62・iCloud entitlements 付与・`codesign --verify` OK）、`flutter run -d macos` も起動・VM Service アタッチまで成功（アプリを前面に出せない警告のみ）。以後 macOS の実行・動作確認を開発側で完結できる
  - セキュリティトレードオフ（ユーザー合意済み）: 開発鍵はローカルでは実質パスワード保護なしになる（Development 証明書のみ・漏洩時は失効可能）

- ✅ 2026-06-11: フェーズ6 所有家電プロンプト改善
  - `buildSuggestPrompt` の所有家電セクションを種別+容量の箇条書き形式に変更（`- hotcook（容量 2.4L）` のように表示。capacity が null なら種別のみ）
  - 家電あり時のみ条件セクションに調理手順指示を追加: 向く料理に家電手順（汎用的な調理モード名・加熱時間を明記）、向かない料理は通常調理でよい、機種固有の自動メニュー名・メニュー番号は出力しないこと
  - 家電なし時はプロンプトの出力が現状から変わらない（指示行を追加しない）
  - JSON スキーマ（cookMode/cookMinutes/appliance フィールド）は変更なし
  - `test/recipe_providers_test.dart` に3件追加: 家電あり+capacity→容量+家電指示確認 / 家電あり+capacity なし→種別のみ / 家電なし→指示行なし確認。計 146 件全パス

- ✅ 2026-06-11: ShelfLifeRule（日持ち目安データ）本格化
  - データパイプライン `tool/shelf_life/`: `foodkeeper_ingredients.csv`（FoodKeeper 2019-06 ミラー・663 行・CC0、python3 urllib で取得・固定化）+ `ja_mapping.json`（FoodKeeper 項目への日本語マッピング 97 + 和食材補完 94）→ `generate.dart`（純 Dart CLI・Flutter 非依存）が `assets/shelf_life/shelf_life_rules.json` を生成
  - 生成ロジック（`shelf_life_compute.dart` に純関数で分離・テスト対象）: 冷蔵中央値 `round((min+max)/2)`、Metric 換算 Days=1/Weeks=7/Months=30/Years=365、保存源フォールバック Refrigerate→DOP_Refrigerate→Pantry→DOP_Pantry、数値化できない Metric（Package use-by date 等）はスキップ。日本語エイリアスを持つ項目のみ出力。**冪等**（再実行で git diff なし）。最終 185 ルール（foodkeeper 91 + supplement 94。FoodKeeper の 6 項目は数値化できる日数が無くスキップ→カテゴリ別フォールバックで代替）
  - 実行時レイヤー `lib/core/shelf_life/shelf_life_table.dart`: `ShelfLifeTable`（同期 `daysFor(name, category)`。照合 ①完全一致→②部分一致最長優先→③normalizedKey→④null）/ `load()`（rootBundle）/ `empty()`。`shelfLifeTableProvider`（既定 empty）を `main()` を async 化して起動時 load → ProviderScope overrides で注入（load 失敗時は empty で起動継続）
  - `shelf_life.dart` に `expiryFromName(table, name, category, from)` を追加（テーブルヒット→日数、ミス→`defaultExpiryFrom` にフォールバック）。既存 `defaultExpiryFrom`（カテゴリ別）は据え置き
  - UI 配線: 食材フォーム（`ingredient_form_screen.dart`）の名前 onChanged・カテゴリ変更で期限を再計算（手動変更済みなら上書きしない・編集モードは既存値維持）/ カメラ確認の `confirm()` も `expiryFromName` 経由
  - `pubspec.yaml` に assets エントリ追加。`tool/shelf_life/README.md`（出典・CC0・公式版差し替え手順）
  - テスト: `shelf_life_generate_test.dart`（17件: Metric 換算・中央値・フォールバック・行照合・CSV パース）+ `shelf_life_table_test.dart`（14件: 照合階層・expiryFromName フォールバック・実アセット読み込みスモークで豆腐/鶏むね肉/もやしヒット）全緑。既存 112 件も全パス

- ✅ 2026-06-11: M8 ヘルプ/このアプリについて完了
  - `help_desktop_view.dart`: 読み物レイアウト（本文最大幅 680px 中央寄せ）。ヒーロー（アプリアイコン+バージョン+一言紹介）/かんたんな使い方（STEP 1〜4）/賞味期限データについて（FoodKeeper 説明+オレンジコールアウト「あくまで目安」）/出典リンク（FoodKeeper・Data.gov、url_launcher で外部ブラウザ起動）/緑コールアウト（賞味期限手動修正）/規約・プライバシー・FAQ（URL 未定のため無効表示 + TODO コメント）/フッター帰属表示
  - `help_version_widget.dart`: package_info_plus でバージョンを取得。取得失敗時は非表示（SizedBox.shrink()）
  - シェルの help セクションを `HelpDesktopView` に置換（プレースホルダ解消）。`_Placeholder` ウィジェット（全セクション実装済みのため不要）を削除
  - `app_shell_test.dart`: 「ヘルプをタップするとプレースホルダが表示される」を「ヘルプビューの主要セクション見出しが表示される」に更新
  - `test/help_desktop_view_test.dart`: 6件（主要セクション見出し・目安コールアウト・STEP 1〜4・緑コールアウト・出典リンク・規約行）全緑
  - l10n（app_en.arb / app_ja.arb）は既にヘルプ文言が追加済みのため `flutter gen-l10n` 不要

- ✅ 2026-06-11: M7 オンボーディング（設定アシスタント）実装・配線・テスト完了
  - `onboarding_desktop_view.dart`: ステップレール（緑グラデーション・現在地/完了表示）+ 6ステップ（ようこそ/AIを選ぶ/連携の許可/リストを選ぶ/調理家電/完了サマリー）。保存は M3 と同じ経路（selectedProvider・SecureStorage・shoppingListId/Name・appliancesJson）
  - 実装サブエージェントがセッション上限で中断 → 書きかけのコンパイルエラー（末尾の迷子 import・未使用パラメータ・deprecated `value`）をレビューで修正し、シェル配線と app_shell_test 更新を実施
  - `test/onboarding_desktop_view_test.dart`: 6件（ようこそ→AI遷移・ステップレール6項目・プロバイダ選択保存・スキップ・完了サマリー・getLists 失敗エラー）全緑

- ✅ 2026-06-11: M6 カメラ登録完了
  - `camera_capture_controller.dart`: 4フェーズ（capture/analyzing/review/error） + `addImages`（長辺1024px・JPEG品質80に縮小）/ `analyze()`（recipeProviderProvider 解決→supportsVision チェック→recognizeIngredients→review 遷移）/ `toggleCandidate` / `updateCandidate`（名前・数量・単位・カテゴリ）/ `confirm()`（checked のみ保存・期限自動セット・capture に戻り在庫へ遷移）/ `reset()`
  - 確信度バケット: ≥0.75=high（初期チェックON）、0.4〜0.75=mid（ON）、<0.4=low（初期チェックOFF・opacity 0.6）
  - `camera_desktop_view.dart`: capture（ドロップゾーン640×380・desktop_drop＋file_selector・サムネイル列・解析ボタン）/ analyzing（パルスアニメーション・進捗バー・他フェーズ遷移時に確実破棄）/ review（左ペイン候補リスト+右ペイン380px編集フォーム+確定ボタン）/ error（noApiKey/noVision/network別メッセージ・設定導線）
  - pubspec.yaml に `file_selector: ^1.0.3`・`desktop_drop: ^0.7.1`・`image: ^4.3.0` を追加（desktop_drop 0.4 は SPM 非対応で CocoaPods が復活したため、レビューで SPM 対応の 0.7.1 へ更新し Podfile を排除。**SPM 化は維持**）
  - entitlements（DebugProfile + Release）に `com.apple.security.files.user-selected.read-only` を追加
  - app_shell: camera セクションを `CameraDesktopView` に置換・⌘R ハンドラに camera 分岐追加（capture フェーズ＋1枚以上で analyze 実行）
  - ARB に M6 文言を en/ja 追加し `flutter gen-l10n`
  - `test/fakes/fake_recipe_provider.dart`: `recognizeResult` / `recognizeError` / `supportsVisionOverride` フィールドを追加（後方互換。デフォルトは既存動作を維持）
  - `test/camera_desktop_view_test.dart`: 9件（capture初期表示・addImages→サムネイル・analyze成功→review/低確信度OFF・候補編集・confirm→DB保存+期限自動セット・meatカテゴリ期限3日・recognizeError→error・noApiKey・noVision）全緑
  - `test/app_shell_test.dart` の「カメラ登録をタップするとプレースホルダが表示される」をドロップゾーン文言の検証に更新

- ✅ 2026-06-11: M5 買い物リスト 2ペイン完了
  - `shopping_confirm_controller.dart`: 5状態（noTarget/listing/adding/done/error） + `initialize()`（mealsForShopping → decidedMeals → noTarget の優先順解決）
  - `shopping_desktop_view.dart`: 左ペイン（不足食材チェックリスト・すべて選択/解除・数量ステッパー）＋右パネル（リマインダーリスト一覧ラジオ・新規リスト作成・追加ボタン）＋完了画面（緑チェック・件数・在庫に戻る）＋対象なし画面（献立提案へ）
  - normalize フォールバック: API キー未登録・通信失敗時は純関数 `findMissingIngredients`（名前一致のみ）で継続。AI 必須にしない
  - `ref.listen` で mealsForShoppingProvider の変化を検知し自動再初期化
  - app_shell の shopping セクション切替先を `ShoppingDesktopView` に変更（プレースホルダ解消）
  - ARB に M5 文言を en/ja 追加し `flutter gen-l10n`
  - `test/shopping_desktop_view_test.dart`: 6件（対象なし/不足食材一覧/チェック解除/追加実行/addItems失敗/数量ステッパー）全緑
  - `test/app_shell_test.dart` を M5 変更（shopping がビューに変わった）に合わせて更新
- ✅ 2026-06-11: M4 献立提案 2ペイン完了（前セッションの実装を検証・修正して確定）
  - `meal_suggestion_controller.dart`: 5状態（提案前/生成中/結果/在庫わずか/エラー）+ `decidedMealsProvider`・`mealsForShoppingProvider`（M5 への引き渡し口）
  - `meals_desktop_view.dart`: 条件チップ・結果リスト＋詳細（材料の在庫色分け・手順）・エラー時の設定導線
  - **バグ修正**: コントローラが `watchInventory().first` で在庫を読んでいたが、①StreamProvider の `.future` はリスナーがいないと解決しない ②drift stream の初回発行は FakeAsync（widget テスト）下で pump しても進まない、の二重の罠で suggest が「生成中」のまま停止していた。`InventoryRepository.getInventory()`（一発クエリ）を新設しコントローラから stream 依存を排除
  - **テストハングの真因解明**: widget テストが1件失敗すると drift の後始末が fake クロック上で完了せず、テストごとに 10 分のウォッチドッグ満了まで停止（スイート全体がハングして見えていた）
  - `meals_desktop_view_test.dart` 6件 + `app_shell_test` の旧プレースホルダ期待を更新。計85件全パス
- ✅ 2026-06-10: M3 設定 2ペインビュー実装
  - `settings_desktop_view.dart`: セクションナビ(200px) + 右コンテンツ。AI設定（4社カード・APIキー保存/マスク/削除・モデル選択=listModels 接続）・一般（言語）・買い物リスト（getLists/createList・現在値表示・失敗時エラー）・調理家電（ホットクック/ヘルシオのピルトグル+型/容量ピッカー → appliancesJson）・データ（同期トグルは無効プレースホルダ）・サポート（Buy Me a Coffee は URL 未定で無効・ヘルプ→shellSection・このアプリについてダイアログ）
  - `SecureStorageService` を `FlutterSecureStorage` 注入可能にリファクタ（テストでフェイク差し替え）。プロバイダ名・Vision 対応・キー取得 URL はハードコードせず実装/定数から取得
  - `url_launcher` ^6.3.0 / `package_info_plus` ^8.0.0 を追加（キー取得ページ外部リンク・About のバージョン表示）
  - app_shell の設定埋め込みを `SettingsDesktopView` に置換（TODO(M3) 解消）。モバイル `SettingsScreen` は据え置き
  - ARB に M3 新規文言を追加（en/ja 両方）し `flutter gen-l10n`
  - `test/settings_desktop_view_test.dart`: 6件（ナビ切替・プロバイダ選択保存・APIキー保存・マスク・言語切替・データ準備中・買い物リスト失敗エラー）全緑
- ✅ 2026-06-10: M2 在庫 3ペインビュー実装
  - `inventory_desktop_view.dart`: 3ペイン構成（カテゴリレール・期限グループリスト・詳細(300px)）。
  - `toolbar_button.dart`: ToolbarButton / ToolbarDivider / ToolbarSearchField（再利用可能共通部品）
  - ツールバー差し替え: 在庫セクション選択時は `InventoryDesktopToolbar`（+食材追加/カメラ/献立提案ボタン+検索）
  - ショートカット追加: ⌘K（camera へ）・⌘R（meals へ）を在庫セクションで有効化
  - `inventory_providers.dart` に `desktopSearchQueryProvider`・`desktopCategoryFilterProvider` を追加
  - ARB に M2 新規文言を追加（en/ja 両方）し `flutter gen-l10n` 適用
  - `test/inventory_desktop_view_test.dart`: 8件（グルーピング・カテゴリフィルタ・行選択・検索・使い切り・空状態など）全緑
  - `test/app_shell_test.dart` を M2 の変更（ツールバーボタン追加）に合わせて修正
- ✅ 2026-06-10: 追加/編集フォームの widget テスト（`ingredient_form_screen_test` — 名前/数量/カスタム単位のバリデーション・正常保存と期限自動セット・編集時の既存値表示と normalizedName 維持。計5件）
- ✅ 2026-06-10: 設定画面の widget テスト（`settings_screen_test` — 言語の現在値表示・English への切替と DB 保存・未設定時は「システムに従う」選択）
- ✅ 2026-06-10: 在庫一覧の widget テスト（空状態・期限グルーピング・左スワイプアクション。drift stream 破棄時の pending timer 対策パターンを確立）
- ✅ 2026-06-10: 不足材料の抽出ロジック（`MissingIngredientsService` — 名寄せ込みの在庫突き合わせ・複数献立の集約・買い物リストアイテム変換）
- ✅ 2026-06-10: normalizedName バックフィル（`NormalizedNameBackfillService` — 未付与行のみ・チャンク分割・冪等）
- ✅ 2026-06-10: 追加 AI プロバイダ実装（Grok / OpenAI = OpenAI 互換共有、Claude = Messages API）
- ✅ 2026-06-10: モデル名の動的取得（`listModels()`）+ ユーザー選択の永続化（`modelOverrides`、DB v3）
- ✅ 2026-06-10: プロンプト・JSON パースの全プロバイダ共通化（`recipe_prompts.dart`）、共通例外、タイムアウト60秒
- ✅ 2026-06-10: DI ファクトリ（`createRecipeProvider` / `recipeProviderProvider`）
- ✅ 2026-06-10: xattr クリアスクリプト削除（iCloud Drive 外に移動したため不要化）
- ✅ 2026-06-10: 設計書・仕様書を実装に同期（v2 基盤レイヤー反映）
- ✅ 2026-06-09: DB v2・ShoppingListService・RecipeProvider 抽象 + Gemini・SecureStorage（コミット 44dafb3）

## 3. 次にやれるタスク

### 画面デザイン不要（今すぐ着手可能）

- [x] normalizedName バックフィルの設計と実装（2026-06-10 完了）
- [x] 不足材料の抽出ロジック（2026-06-10 完了）
- [x] widget テスト: 在庫一覧（空状態・期限グルーピング・スワイプ）（2026-06-10 完了）
- [x] ShelfLifeRule の本格化: USDA FoodKeeper（2019-06 ミラー・CC0）の取得・整形 → 日本語マッピング（97 項目）→ 和食材補完（94 項目）→ JSON 同梱（ビルド時生成スクリプト `tool/shelf_life/generate.dart`）。実行時は `ShelfLifeTable` が食材名→日数を引く（2026-06-11 完了）
- [x] widget テスト: 設定の言語切替（2026-06-10 完了）
- [x] widget テストの追加拡充（追加/編集フォームのバリデーション）（2026-06-10 完了）

### macOS UI 実装計画（2026-06-10 デザイン到着 → 着手）

参照: `design_handoff/src/macosShell.jsx`（シェル）/ `macosApp.jsx`（全7画面）/ `shared.jsx`（トークン・共通部品）/ `macosOnboarding.jsx` / `README.md`。HTML プロトタイプを **Flutter で**忠実に再実装する（README は SwiftUI 推奨だが本プロジェクトは Flutter。CLAUDE.md の画面幅分岐方針に従い、広い幅 = デスクトップシェルとして実装し、狭い幅は従来のモバイルフローを維持）。

| # | フェーズ | 内容 | 状態 |
|---|---|---|---|
| M1 | アプリシェル | サイドバー(216px・ナビ7項目・⌘1–4/⌘,)＋ツールバー(50px)＋コンテンツ。フォントは既存の google_fonts 適用で充足 | ✅ 2026-06-10 |
| M2 | 在庫 3ペイン | カテゴリフィルタ(168px)＋期限グループ付きリスト＋詳細(300px)。ホバーアクション・検索・行選択・ツールバーアクション | ✅ 2026-06-10 |
| M3 | 設定 2ペイン拡張 | セクションナビ(200px)＋AI設定（プロバイダ4社カード・APIキー・モデル選択=listModels接続）・一般（言語）・買い物リスト・調理家電・データ・サポート | ✅ 2026-06-10 |
| M4 | 献立提案 2ペイン | 条件チップ・生成中・結果リスト＋詳細（材料の在庫色分け・手順）・在庫わずか・エラー。`recipeProviderProvider` 接続 | ✅ 2026-06-11 |
| M5 | 買い物リスト 2ペイン | 不足食材チェックリスト＋数量ステッパー／追加先リマインダーリスト選択／追加中・完了・エラー。`MissingIngredientsService`+`ShoppingListService` 接続 | ✅ 2026-06-11 |
| M6 | カメラ登録 | ドロップゾーン→解析中→二ペイン候補確認→確定で在庫へ（必ず確認画面経由・画像縮小・最大10枚） | ✅ 2026-06-11 |
| M7 | オンボーディング | 設定アシスタント6ステップ（ステップレール・完了サマリー） | ✅ 2026-06-11（テスト完了） |
| M8 | ヘルプ/このアプリについて | 読み物レイアウト・FoodKeeper 出典表記 | ✅ 2026-06-11 |

> 順序の意図: M3（設定=APIキー入力 UI）を M4（献立提案）より先にやるのは、献立提案の動作確認に API キーの登録手段が必要なため。M6 以降は AI 動作確認後。

---

## 4. 要検討事項

### 設計上の未決定（❓）

| 項目 | 内容 | 決めるタイミング |
|---|---|---|
| API キー管理 UI | 設定画面への組み込み方（入力・検証・削除、キー取得ページへのリンク） | 設定画面デザイン到着時 |
| Vision 非対応モデル選択時の扱い | `supportsVision` を listModels の情報からモデル単位で判定するか、選択時に警告するか | モデル選択 UI 実装時 |
| バックフィルの実行トリガ | 機構は実装済み（一括・冪等）。献立提案フロー後のバックグラウンド実行を推奨 → UI 接続時に配線して確定 | 献立提案 UI 実装時 |
| 買ったものを在庫へ戻す方式 | リマインダー完了の監視は不可。手動ショートカットの形を検討 | 買い物リスト機能の後 |
| 同期方式（iOS） | **決定済み（2026-06-11）**: iCloud ubiquity コンテナの隠し領域に丸ごと JSON バックアップ。自動（デバウンス）+手動、復元は手動+確認、競合は単一ファイル上書き（last-write-wins）。API キーは iCloud キーチェーン同期。詳細は §2 のフェーズ8エントリ | 決定済み |
| カメラの画像縮小値 | 長辺1024px・JPEG品質80 で暫定決定（M6）。将来の調整は `_resizeToJpeg()` の定数変更のみ | 決定済み（M6） |
| サブスク proxy 対応 | 自前キー以外の課金モデルを将来やるか（現状: 全機能無料・自前キー） | リリース後 |
| 食材ごとの絵文字 | デザインは食材単位の絵文字を表示するが DB に列がない。当面カテゴリ絵文字で代替し、AI 連携（認識・名寄せ）時に絵文字付与を検討 | M2 実装中に暫定決定済み（カテゴリ代替）、最終は AI 連携時 |
| カテゴリ区分の差 | デザインは6カテゴリ（肉/魚/野菜/乳製品/調味料/常備品）、実装 enum は12種。表示は12種のまま進め、デザイン側の意図（集約）が必要なら後で検討 | M2 実装時 |
| データ同期トグル | **解消（2026-06-11）**: データセクションに syncEnabled トグル+バックアップ/復元 UI を実装済み（フェーズ8） | 決定済み |
| オンボーディングの初回起動時自動表示 | 現状はサイドバーから手動起動のみ。初回起動判定（設定に onboardingDone 列追加 = マイグレーション）を入れるか | M7 テスト完成後 |
| ヘルプの検索フィールド | helpAbout.jsx のデザインには検索フィールドが描かれているが機能仕様が未定義のため M8 では実装しなかった。将来のコンテンツ拡充後に検討する | コンテンツ量が増えた段階 |
| **カメラ（モバイル）の途中再開** | 再入場時、capture/review の途中状態（撮影済み写真・編集中の候補）をアプリ生存中保持して再開する現仕様で良いか、毎回リセットすべきか。error のみ 2026-06-12 のレビュー修正で入場時自動リセット済み | ユーザー判断待ち（実機で触って決めるのが良い） |
| **同期トグルの失敗時挙動** | iCloud 未サインイン等で ON 直後の即時バックアップが失敗してもトグルは ON のまま（エラーは SnackBar 表示。後で iCloud が使えるようになれば自動バックアップが効き始める）。OFF に巻き戻す方が誤解が少ないか。デスクトップ/モバイル共通 | ユーザー判断待ち |
| 設定の API キー行の状態表示 | モバイル設定の「APIキー」行に登録済み/未登録の表示がない（タップして開くまで分からない）。非同期読み出しが要るため見送り中 | 任意の改善 |

### ShelfLifeRule（FoodKeeper データ）の課題 — 2026-06-10 調査結果 → ✅ 2026-06-11 実装完了

> **2026-06-11 完了**: 下記の決定事項どおり、ミラー CSV（2019-06・CC0）+ 開発時マッピング（FoodKeeper 97 + 和食材補完 94）で実装。`tool/shelf_life/generate.dart` が `assets/shelf_life/shelf_life_rules.json`（185 ルール）を生成・同梱し、実行時は `ShelfLifeTable` が参照する。公式最新版に差し替えたくなったら `tool/shelf_life/README.md` の手順（ユーザーがブラウザで公式 JSON/XLS を入手 → 同ディレクトリに置く → 再生成）に従う。詳細は §2 の完了エントリ参照。

公式 USDA 配布元（fsis.usda.gov の EN/ES/PT JSON・XLS、CC0 ライセンス）が**この開発環境から Akamai により 403 でブロック**され、自動取得できない（curl の UA 変更・WebFetch とも不可。data.gov のリソース URL は 404）。

- 代替として GitHub ミラー `jelera/food-shelflife-db` の `lib/seeds/ingredients.csv`（663行、FoodKeeper スキーマ: Pantry/Refrigerate/Freeze の Min/Max/Metric + 開封後・解凍後。データは 2019-06 時点の FoodKeeper 由来）が raw.githubusercontent.com から取得可能なことを確認済み。
- **決定事項（2026-06-11 ユーザー判断）**:
  1. **取得経路**: ✅ ミラー CSV で実装を進め、公式版をユーザーがブラウザで入手できたら入力を差し替えて再生成する（差し替え前提のパイプライン構造にする）。FoodKeeper はデータ更新頻度が低く、本アプリの用途は「目安」のため 2019 年版で実用上問題なしと判断
  2. **日本語マッピング方式**: ✅ 開発時に Claude が対訳（日本語名・normalizedName → FoodKeeper 項目）を作成してコミットし、人手レビュー可能にする
  3. **AI 和食材補完の実行方法**: ✅ ビルド時に API は叩かない。FoodKeeper にない和食材（豆腐・納豆・味噌等）は開発時に Claude が日数データを作成してコミット
  4. **出典表記**: ✅ ヘルプ画面に USDA FoodKeeper 出典 + 「あくまで目安」を実装済み（M8）

### ユーザー（futo4）にお願いしたいこと — 2026-06-13 時点 (更新)

開発側（Claude Code）では実施できない・判断できないもの。上から優先度順。

1. **実 API での動作確認（最優先）**: Gemini API キーを [Google AI Studio](https://aistudio.google.com/apikey) で取得し、アプリの設定画面（または設定アシスタント）から登録 → 献立提案・カメラ登録（実画像）を一通り試す。M3〜M6 の UI は AI 呼び出しを実 API で未検証。特に **家電出し分け（ホットクック/ヘルシオ設定時に steps に汎用モード名・加熱時間が入るか、機種固有メニュー番号が出ないか）** の品質確認も含める
2. **macOS 実機での連携確認**: 買い物リスト追加を実行してリマインダーのアクセス許可ダイアログ → リスト追加まで確認（platform channel はテスト環境で検証不能）
3. **FoodKeeper 公式最新データの入手**（差し替え用・急ぎではない・任意）: 実装は GitHub ミラー（2019-06 版）で**完了済み**（2026-06-11。§2・§4 参照）。アプリは現状このミラーデータで動作する。公式の最新版に差し替えたくなったら、開発環境からは fsis.usda.gov が 403 のため、以下をユーザーのブラウザで実施してほしい（手順詳細は `tool/shelf_life/README.md`）:
   1. [Data.gov の FSIS FoodKeeper Data ページ](https://catalog.data.gov/dataset/fsis-foodkeeper-data) を開く（または fsis.usda.gov 内で「FoodKeeper Data」を検索）
   2. リソース一覧から **English の JSON（または XLS）** をダウンロードする（CC0 ライセンス。ES/PT 版は不要）
   3. ダウンロードしたファイルを `tool/shelf_life/`（生成パイプラインの入力置き場）に置き、Claude Code に「公式データに差し替えて再生成して」と伝える — パイプラインが整形・JSON 再生成・テストまで行う
4. **CocoaPods のアンインストール（任意）**: M6 で一時的に必要になり `brew install cocoapods` されたが、desktop_drop 0.7.1（SPM対応）への更新で不要になった。`brew uninstall cocoapods` で削除してよい → 実施完了
5. **Buy Me a Coffee URL の作成**: ドネーション導線（設定 > サポート）の実装を完了させる際に必要
6. ~~**Xcode 署名設定**~~ → **✅ 実施完了（2026-06-11）**: Team 署名 + iCloud capability 設定済み。さらに 2026-06-12 に専用署名キーチェーン（`tool/macos_signing/setup_signing_keychain.sh`）をセットアップし、開発環境シェルからも署名ビルド・`flutter run -d macos` 可になった。当時の手順（参考）:
   1. Xcode で `macos/Runner.xcworkspace` を開く
   2. 左のプロジェクトナビゲータで **Runner** プロジェクト → **Runner** ターゲット → **Signing & Capabilities** タブ
   3. **「Automatically manage signing」にチェック**を入れ、**Team** に登録した Apple Developer の Team を選択（Debug/Release 両方）
   4. Capabilities に **iCloud**（コンテナ `iCloud.com.futo4.tsukaikiri`）が表示されることを確認。出ていなければ「+ Capability」から iCloud を追加し、iCloud Documents にチェック + コンテナ `iCloud.com.futo4.tsukaikiri` を作成/選択（Xcode が Developer ポータルに App ID とコンテナを自動登録する）
   5. Xcode で一度ビルド（⌘B）して署名が通ることを確認 → 以後 `flutter run -d macos` も通るようになる
   6. 動作確認: Mac が iCloud にサインインした状態でアプリの 設定 > データ から「今すぐバックアップ」→「バックアップから復元」を試す（このとき pbxproj に DEVELOPMENT_TEAM の差分が出るのでコミットして OK）

> 済: `test/probe_meal_controller_test.dart` の削除（2026-06-11 ユーザー実施）/ `macos/Pods`・`Podfile` の削除（2026-06-11 開発側で実施）

**2026-06-13 追加 (コードレビュー後)**: 実装済みコードの静的品質は非常に高く、3件のエッジ指摘以外は設計・CLAUDE.md 忠実。実機/実API 確認時に上記 3件の挙動（camera confirm エラー時、未知プロバイダ選択+キー時、Reminders 外部削除時リスト選択）を特に注視してください。

### ユーザー抜きで進められる作業（開発側/Claude Code が自律的に着手可能）
**定義**: 実APIキー取得・物理実機タッチ・外部URL作成・アカウント発行・画像アセット作成など「ユーザー提供」が不要な作業。Flutter コード・テスト・ドキュメント・準備コメントなどで即時対応可能。CLAUDE.md「ユーザーとのやり取りは常に日本語」および進捗管理ルールに従い、リストはここに永続化。

優先度: 高（即価値大・他機能に波及小）→ 中 → 低。ファイル/場所の目安付き。

#### 高優先（すぐに着手して効果が出やすい）
1. **Help / Onboarding のモバイル（狭い幅）版実装**
   - 現状: `help_desktop_view.dart` / `onboarding_desktop_view.dart` のみ。`app_shell.dart` の `_ContentArea` と narrow フロー（main.dart LayoutBuilder + InventoryListScreen 中心）が desktop view に依存。
   - 作業: `HelpMobileView` / `OnboardingMobileView` を新設（コントローラ/ロジックは desktop と完全共有、パターンとして meals/shopping/camera_mobile_view と同じ）。shell や settings からの遷移を narrow でも自然に。
   - 関連: モバイルナビや push フロー整備。テスト追加。
   - ファイル目安: `lib/features/help/presentation/help_mobile_view.dart`（新規）、同 onboarding、app_shell 調整、既存テスト拡張。

2. **在庫詳細「レシピを見る」の機能化（comingSoon 脱却）**
   - 現状: `ingredient_detail_view.dart` で `toast(l10n.comingSoon)` のみ。`inventory_list_screen.dart` にも _comingSoon あり。
   - 作業: 選択食材を `MealSuggestionController` や meals 画面の初期条件として引き渡す（`RecipeConstraints` や decided 相当のフィルタ）。AI 呼び出し自体は後回しで「在庫ベースの提案開始」だけでも価値あり。TODO(M4) `inventory_desktop_view.dart:193` も関連。
   - ファイル: `lib/features/inventory/presentation/widgets/ingredient_detail_view.dart`、`inventory_desktop_view.dart`、`features/meals/` 側調整。

3. **モバイル設定の残 comingSoon 解消 + プレースホルダ改善**
   - 現状: `settings_screen.dart`（モバイル）で Buy Me a Coffee / Help / About が _comingSoon。データ同期は既に実装済み。
   - 作業: Help は shell または専用画面遷移に、About はダイアログ実装（desktop 準拠）。Buy Me a Coffee は URL 未定なので「将来の URL 注入ポイント」を用意しつつ「準備中」表示を洗練。`settingsSupportComingSoon` などの l10n 活用。
   - ファイル: `lib/features/settings/presentation/settings_screen.dart` + 関連サブ画面。

4. **テスト・エッジケースの大幅拡充**
   - 現状: ~200件全パス・analyze 0 だが、カバーしきれていないエッジ（restore 後の未知 provider、camera confirm の save 失敗シミュ、sync fingerprint 境界、shelf life 照合失敗フォールバック、normalizedName バックフィルなど）。
   - 作業: `test/` 下で Fake や memory DB を使って追加。`camera_capture_controller_test` 強化、`sync/` テスト拡充、`expiry_status_test` など純粋関数テスト。
   - ファイル: 新規/既存 test/*_test.dart（特に controller と service 層）。

5. **カメラ途中再開 / 同期トグル失敗挙動 の「コード基盤」準備**
   - 現状: 進捗 §4 で「ユーザー判断待ち（実機で触って決める）」と明記。実装は未。
   - 作業: `CameraCaptureController` に `preserveStateOnBackground` フラグや一時保存機構を追加（デフォルトは現行の error のみリセット）。Sync 側に失敗時自動 OFF オプションやより良い UX トランジションを追加（設定で切替可能に基盤）。
   - ファイル: `camera_capture_controller.dart`、`sync_controller.dart`、`settings` 関連。コメントに両 UX のトレードオフを記述。

#### 中優先（準備価値が高い）
- **iOS / Android 事前準備コード（Flutter レイヤー）**
  - `ios/Runner/` (Info.plist, AppDelegate, bridging) への EventKit / iCloud コメント拡充と登録準備。
  - Android: `AndroidManifest.xml` permission、gradle への google_sign_in 準備コメント。`ShoppingListService` / `SyncService` の Android impl スケルトン開始（抽象は既にある）。
  - ファイル: `ios/Runner/`, `android/app/src/main/`, `lib/features/shopping/service/`, `lib/features/sync/service/`.

- **Help 画面の M8 TODO コード整備**
  - 規約/プライバシー/FAQ の 3 URL（`help_desktop_view.dart:241-258` の TODO(M8)）。URL 自体はユーザー提供だが、` _LegalRow` を url=null 時も将来 url_launcher 呼び出し可能に整備し、タップ時の「準備中」挙動を一貫させる。モバイル版でも同じコンポーネント再利用。
  - その他ヘルプコンテンツの質向上（ステップ詳細追加など、URL 不要）。

- **TODO(M4) の選択食材引き渡し完全配線**（上記 2 と重複するが独立でも可）。
- **フォント配信方式の準備**
  - 現状 google_fonts 実行時取得。オフライン初回フォールバック改善や assets への移行実験（pubspec 変更 + ロードコード）。
  - ファイル: `pubspec.yaml`, `main.dart`, `core/theme/`.

- **l10n / 国際化の網羅強化 + ハードコード除去**。
- **対応言語追加 (スペイン語 'es')**: UI 言語として追加（ARB, LocaleController, 言語選択画面）。AI 出力は ja/en fallback のまま（prompts 拡張別途）。中優先 i18n として作業アイテム追加。テスト拡充も。

#### 低優先 / 運用・保守系（隙間時間で）
- ドキュメント: 本セクションのメンテ + `doc/設計メモ` や `CLAUDE.md` への反映、help テキスト拡充。
- コード品質: さらなる重複除去、コメント追加、定数集約（appliance など既にあるが拡大）。
- 追加の純粋関数テスト（`missing_ingredients_test.dart` 拡張など）。
- `tool/shelf_life/` や他の CLI ツールの改善。

**このリストの使い方**:
- ユーザーが「この中から X をやって」と指示 → 即着手（notes/進捗更新しながら）。
- 定期的にこのセクションを見直し、ユーザー依存タスクが解消されたら一部を「高優先」に昇格。
- 2026-06-13 時点で flutter analyze 0 / 全テストパスを維持しながら進める。

**2026-06-13 追加**: 上記リストは notes.md での探索（grep TODO/comingSoon + app_shell + help + 進捗 §4 突合）に基づき作成。詳細判断は同 notes に記録。

**2026-06-13 計画 & 着手追記**:
- ユーザー指示により「高優先から作業計画立案 + 着手」を開始。
- 詳細計画は notes.md 「高優先ユーザー抜き作業 計画立案 & 着手」セクションに記載。
- 優先順: 
  1. Helpモバイル実装（設定モバイルHelp comingSoon 即時解除）→ **完了**（help_content共有 + mobile view + 配線）。
  2. Onboardingモバイル。
  3. レシピを見る機能化。
  4. その他テスト/基盤。
- 各Phaseで notes/本ファイル更新 + analyze/test + 通知。
- 現在の状態: Phase 1-3 **完了**。Phase 4 (モバイル設定残) **完了**。Phase 5 (テスト拡充) **着手中** + 新: 対応言語にスペイン語追加を中優先として作業アイテムに追加・着手。notesに記録。

### コードレビュー指摘（2026-06-14・オンデバイス AI 移行 `a39e132..HEAD`）

`/code-review`（高負荷・4角度ファインダ）でオンデバイス AI 移行の差分（47ファイル）をレビュー。重大度順。✅=修正済 / 🔜=未着手 / ❓=要判断。

| # | 重大度 | 指摘 | 場所 | 状態 |
|---|---|---|---|---|
| 1 | 🔴 高 | カメラ入口を `aiAvailableProvider`（非null）だけで判定し `supportsVision` を見ていない。オンデバイス既定（vision 非対応）構成では入口は開くが解析時に必ず `noVision` | `camera_desktop_view.dart` / `camera_mobile_view.dart` | ✅ 2026-06-14 |
| 2 | 🔴 高 | オンデバイス生成の失敗（空出力・非JSON・Android DL中 `generate_failed` 等）がすべて `MealSuggestionError.network` にマップされ「Wi-Fi に接続」と誤案内 | `meal_suggestion_controller.dart` | ✅ 2026-06-14 |
| 3 | 🟠 中 | クラウドプロバイダ選択＋キー未登録時に無言でオンデバイスへフォールバック。設定 UI は選択クラウドを表示したまま（実エンジンと不一致） | `core/providers.dart` | ✅ 2026-06-14 |
| 4 | 🟠 中 | `aiAvailableProvider` を `orElse: () => true` で読むため、解決が**エラー**になった端末でも「使える」扱いになり案内が出ず、押すとエラーになるボタンが残る | `meals_desktop_view.dart` ほか4ビュー | ✅ 2026-06-14 |
| 5 | 🟠 中 | Android の `maxOutputTokens=1024` が normalize/suggest/recognize 一律。複数レシピ JSON が切れて `FormatException`→（#2 により）毎回ネットワークエラー表示 | `OnDeviceAiPlugin.kt:96` | ✅ 2026-06-14 |
| 6 | 🟡 低 | `recognizeIngredients` が共通の `RecipeProviderException` でなく `UnsupportedError` を投げる（設計方針違反・将来 catch 漏れでクラッシュ） | `on_device_recipe_provider.dart:70` | ✅ 2026-06-14 |
| 7 | ⚪ 整理 | iOS/macOS の `OnDeviceAiPlugin.swift`（123/124行）がコメント1行違いでほぼ完全重複 | `*/Runner/OnDeviceAiPlugin.swift` | ✅ 2026-06-14（部分） |
| 8 | ⚪ 整理 | オンデバイス可用性を `main()`（起動ブロッキング）・`recipeProviderProvider`・`onDeviceAiAvailabilityProvider` の3経路で重複プローブ。キャッシュ済み FutureProvider に集約可 | `main.dart` / `providers.dart` | ✅ 2026-06-14 |
| 9 | ⚪ 整理 | `maybeWhen(data:..., orElse: () => true)` が4ビューにコピペ。派生 Provider 1本に集約すれば #4 も一箇所修正で済む | meals/camera ×2 | ✅ 2026-06-14 |
| 10 | ⚪ 整理 | オンデバイス移行で旧 `modelOverrides`・Keychain のクラウドキーが残置（移行/クリーンアップ経路なし）。後でクラウド再選択時に古いモデル ID が復活し得る | `settings_repository.dart` | ✅ 2026-06-14 |

**整理系の対応（2026-06-14）**:
- #8: `main()`・provider 解決・設定 UI が `onDeviceAiAvailabilityProvider`（キャッシュ済み FutureProvider）を共有。起動時に温めた1回のプローブを使い回し、二重プローブを解消。
- #9: `aiEntryEnabledProvider` / `cameraEntryEnabledProvider`（ローディング=true・エラー=false 方針を集約）を新設し、meals/camera 4ビューの `maybeWhen` コピペを置換。
- #10: API キー削除時に当該プロバイダの `modelOverride` も解除（キー無しでは使えず、再選択時に古いモデル ID が蘇るのを防ぐ）。desktop/mobile 両 `_delete()`。**残置キー自体は #3 方針どおり保持**（ユーザーが再登録し得るため自動削除しない）。
- #7: **部分対応**。別 Xcode プロジェクト2つにまたがる共有ファイル化は iOS ビルドをこの環境で検証できず、本プロジェクトの「プラットフォーム別コピー」慣習にも反するため見送り。代わりに両ファイルを**バイト完全一致**にし、「同期必須」の警告ヘッダーを両方へ追加（finder の懸念＝片側編集での無言乖離を抑止）。完全な共有ファイル化は iOS ビルド検証が可能になった段階で実施。
- 検証: **flutter analyze 0 / 全 251 テストパス / macOS debug ビルド成功**。

**#3 の決定（2026-06-14 ユーザー判断）**: 「クラウド選択＋キー未登録/無効」では**オンデバイスへフォールバックしない**。クラウドが選択されている旨＋「API キー未登録/無効」エラーを出し、ユーザー自身に設定でキー登録 or オンデバイス切替を促す。
- 実装: 解決を `aiResolutionProvider`（`{provider, status}` レコード）に集約。`AiStatus { available, cloudKeyMissing, unavailable }` を導入。`recipeProviderProvider`/`aiStatusProvider` はそれを射影。
- クラウド選択＋キー無し → `provider=null` / `status=cloudKeyMissing`（オンデバイスに落とさない）。
- `AiUnavailableNotice` を ConsumerWidget 化し、`aiStatusProvider` で文言を出し分け（cloudKeyMissing=`aiCloudKeyMissing*` / unavailable=`aiUnavailable*`・en/ja/es 追加）。`title`/`body` 指定時はそちら優先（カメラ vision 文言と共存）。
- テスト: 解決テスト②を「クラウド＋キー無し→null」に変更、⑨ cloudKeyMissing を追加。"AI 非対応端末" widget テスト2件は `aiStatusProvider=unavailable` を override。**全 251 テストパス・analyze 0**。

**2026-06-14 修正実施（#1/#2/#4/#5/#6）**: TDD で対応。
- #1: `aiVisionAvailableProvider`（解決した provider の `supportsVision`）を新設し、カメラ desktop/mobile の入口を vision で出し分け。vision 非対応だが AI は使える端末には専用文言（`cameraVisionUnavailable*`・en/ja/es）を表示。
- #2: `MealSuggestionError.onDeviceFailed` を追加。`suggest()` で `RecipeProviderException.provider == onDeviceProviderId` のときネットワークでなく専用文言（`mealsErrorOnDevice`）に分岐。
- #4: 4ビューの `maybeWhen(data, orElse: () => true)` を `maybeWhen(data, loading: () => true, orElse: () => false)` に（ローディングは誤フラッシュ防止で true、**エラーは案内を出すため false**）。
- #5: Android `maxOutputTokens` を 1024→2048（Gemini Nano 総コンテキスト内で出力枠拡大）。
- #6: `recognizeIngredients` の vision 非対応ガードを `UnsupportedError`→`RecipeProviderException` に。
- 回帰テスト: `meal_suggestion_controller_test.dart`（オンデバイス/クラウドのエラー分類2件）+ `recipe_provider_resolution_test.dart` に vision ゲーティング3件 + `on_device_ai_provider_test.dart` の例外型を更新。既存カメラ capture テスト4件は vision 可フェイクに更新。
- 検証: **flutter analyze 0 / 全 250 テストパス**（gen-l10n 反映）。残: #3（要判断）・#7〜#10（整理）。

### ユーザー抜き作業の進捗（2026-06-14 追記）

実機検証なしで進められるタスクを A→ 順に確認・実施:
- **A 在庫詳細「レシピを見る」機能化**: ✅ **既に実装済み**（`ingredient_detail_view.dart:213-229` が `suggestFromIngredient` + desktop=shellSection / mobile=Navigator push で配線済み。バックログ記述が古かった）。
- **B モバイル設定の comingSoon 解消**: ✅ **ほぼ済み**（Help→HelpMobileView / Onboarding→OnboardingMobileView / About→ダイアログは配線済み。残るは Buy Me a Coffee のみ＝URL 待ちで意図的に準備中表示）。
- **C モバイル在庫の検索**: ✅ **2026-06-14 実装**（TDD）。`inventory_list_screen.dart` の検索アイコンが `_comingSoon` だったのを実機能化。検索欄（`_SearchField`・autofocus・×で解除）をローカル状態で開閉し、名前の部分一致（大文字小文字無視・デスクトップと同ロジック）で絞り込み。一致なしは `_SearchEmpty`（`inventorySearchEmpty({query})`・en/ja/es）。検索クエリは画面ローカル状態（desktop と違い同一 widget 内のため provider 不要）。テスト +3（開閉・絞り込み・該当なし）。analyze 0 / 全 254 テスト緑。
- 残（実機/ユーザー必要 or 大粒）: D テスト拡充（任意）・E Android 実装スケルトン（OAuth/実機が要るため骨組み止まり＝価値小・優先度低）。

### 運用・リリース系

- [ ] アプリアイコン作成
- [ ] Buy Me a Coffee URL の作成（ドネーション実装時）
- [ ] Apple Developer アカウント（実機リマインダーテスト時）
- [ ] Google Cloud 設定（Tasks/Drive API・OAuth クライアント。Android 着手時）
- [ ] クラッシュ収集・アナリティクス「なし」の最終確定（データ収集なし方針）
- [ ] フォント配信方式の最終確認: 現状 google_fonts の実行時取得（キャッシュあり）。オフライン初回起動はシステムフォントにフォールバックするため、リリース前に assets 同梱への切替を検討
- [ ] フォールバックモデル ID の定期見直し（gemini-2.0-flash / grok-4.3 / gpt-5-mini / claude-opus-4-8。2026-06 時点 — 各社の廃止アナウンスに注意）

---

## 5. 品質状態

| 項目 | 状態 | 確認日 |
|---|---|---|
| `flutter analyze` | クリーン（0件） | 2026-06-14（レビュー指摘修正後） |
| `flutter test` | 251 件 全パス（オンデバイス AI レビュー指摘 #1〜#6 の回帰テスト含む） | 2026-06-14 |
| macOS ビルド（debug/release） | debug 成功（2026-06-14 レビュー修正後も確認）+ **実機起動・iCloud バックアップ/復元の動作確認済み**。開発環境シェルからも専用署名キーチェーン（tsukaikiri-ci）解錠で署名ビルド・`flutter run -d macos` 可（CLAUDE.md「macOS の署名」参照。SPM のみ） | 2026-06-14 |
| iOS ビルド | 未確認 | — |
| Android ビルド | 未確認 | — |

**2026-06-13 追記**: バグスキャン実施により静的品質さらに検証済み (notes.md 参照)。指摘4件は即修正済み（本エントリ）。実API/実機検証時は特に「未知プロバイダ選択時・カメラconfirmエラー時・外部リスト削除後のstale選択」の挙動を確認してください。

**2026-06-13 Phase5 テスト拡充 追記 (plan.md 実行継続)**:
- codegen 修復: phase6 (cameraPreserveState/syncKeepOnFailure + SettingsTable v4) 後の `dart run build_runner build` 漏れを発見・即時修復 (215 outputs, .g.dart 刷新)。これにより settings テストロードエラー解消。
- 優先1 (settings support) + 優先2 (meals focus banner UI) は前回完了済み (drag+ja+existence 簡易テストでロバスト化、+3 passing)。
- **優先3 (OnboardingMobileView full coverage) 完了**: test/onboarding_desktop_view_test.dart に pumpMobileView 流用の 4 ケース追加 (⑨ list load成功/empty、⑩ appliance Hotcook/Healsio/持っていない トグル、⑪ finish サマリー+開始ボタン、⑫ failing list error)。desktop ⑤/⑥ パターン厳密再利用、_Fake/_Failing 注入、narrow 400x800 + unmount 1ms + ja set。+4 件、既存モバイル含め Phase2 の全ステップ/エッジを widget でカバー。flutter test (onboarding) 全パス、analyze 0 維持。
- Spanish (es) 中優先: 完了済み (ARB + LocaleController + settings 選択 + gen-l10n + テスト 'Español' ケース)。UI 言語として選択可能 (AI 出力は未拡張)。
- 品質: 全テスト ~210+ 件規模で緑継続。Phase5 テスト拡充は plan 優先3 まで実行。残 (help mobile 拡充、inventory flow、明示 edges、controller 等) は次回継続可能。
- 進捗管理・notes 両方更新 (30分/着手/完了ルール遵守)。ユーザー抜き高優先のテスト項目を着実消化。