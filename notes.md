# notes.md — 作業メモ（セッション横断）

> 運用: 重要な判断・試したこと・失敗・残タスクを記録。完了作業は2〜3行に圧縮。30分ごとに更新し、当初ゴールとのズレを自己確認する。

## 現在のゴール（2026-06-12）

ユーザーが MacBook を操作できない間、**自律的に進められる作業 = モバイル幅 UI の拡充**を進める（iOS 対応の前提作業）。実装は Opus サブエージェント、メインセッションは設計・監査・レビュー（CLAUDE.md のモデル方針）。

## 完了（圧縮済み）

- 2026-06-12: 専用署名キーチェーン `tsukaikiri-ci` で開発シェルから署名ビルド可能に（4a4e528）。署名系コマンドはサンドボックス無効必須。手順は CLAUDE.md「macOS の署名」
- 2026-06-12: 起動時ウィンドウ 1280×850 化（bb46569）。犯人は OS の状態復元 → `isRestorable = false` で解決
- 2026-06-12: 献立提案モバイル版（631bf7b・テスト4件）と買い物リスト確認モバイル版（7a9dc07・テスト5件）。コントローラはデスクトップと共有、計174件全パス

## 完了（続き）

- 2026-06-12: カメラ登録モバイル版完成。サブエージェントがセッション上限で死亡したが部分実装（view 1293行）は良品質だったので採用し、メインセッションで完成（エラー画面の設定導線修正・onCamera 配線3箇所・テスト7件）。計181件全パス
- 判断メモ: 画像入力は当面 file_selector（`_pickImages` 関数に分離済み、iOS フェーズで image_picker に差し替え）。エラー時「再試行」= reset()（画像クリア）はデスクトップ版と同一 UX なので維持
- ユーザーが CLAUDE.md から「モデル使い分け方針」を削除 → 以後メインセッションで直接実装してよい

## 完了（続き2）

- 2026-06-12: 在庫詳細「買い物リストに追加」実機能化（06732e3・テスト3件）。「レシピを見る」はデザイン仕様でも準備中トーストなので現状維持
- 発見: **モバイル設定画面は言語以外すべて comingSoon スタブ**（AI・連携・同期・サポート）。これが iOS 前の最大ギャップ → セクション単位で移植中

## 完了（続き3）: モバイル設定画面の実機能化（2026-06-12）

- AI（5522bbe）・連携=買い物リスト/家電（3410fff）・データ同期（次コミット）の3セクションをサブ画面方式で実装。デスクトップ版とデータ操作を共有、テスト14件追加（計198件）
- 学び: Riverpod v3 AsyncValue に `valueOrNull` なし → `.value` / analyze はテスト側のコンパイルエラーを拾わない / 画面に drift stream watch を足すと既存テストの `pump()`（経過0）unmount が pending timer で落ちる → `pump(1ms)` / ScaffoldMessenger はスナックバーをキューするので連続表示のテストは間に5秒 pump

## 残タスク（次セッション向け）

1. モバイル設定サポート行（ヘルプ画面モバイル版 = helpAbout.jsx、About）— フェーズ9（BMC URL はユーザー待ち）と一緒にやるのが自然
2. オンボーディング（設定アシスタント）のモバイル版・初回自動表示の検討（進捗管理 §4）
3. iOS フェーズ: カメラ画像入力を image_picker に差し替え（camera_mobile_view.dart の `_pickImages` を差し替えるだけ）

## ユーザー待ちの事項（再開時に確認）

- 実 API 動作確認（Gemini キー取得 → 献立提案・カメラ実画像・家電出し分け品質）
- macOS 実機リマインダー連携確認
- （テストデータ: テスト豆腐/テスト牛乳が在庫に残っていれば削除）

## 環境メモ（ハマりどころ）

- flutter analyze/test もサンドボックスに弾かれることがある（flutter キャッシュ書き込み）→ サンドボックス無効で実行
- widget テスト1件失敗でスイートが10分ハングして見える（CLAUDE.md テスト規約）
